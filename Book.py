class Book:
    def __init__(self, book_id, max_price, price, title, url):
        self.book_id = book_id
        self.max_price = max_price
        self.price = price
        self.title = title
        self.url = url